# Production build config (used automatically by `vite build` on Vercel).
# These override the dev values in .env. No secrets here — safe to commit.
# Point these at your Render backend's public URL.
VITE_API_URL=https://ai-interview-portal-1.onrender.com
VITE_WS_URL=wss://ai-interview-portal-1.onrender.com
